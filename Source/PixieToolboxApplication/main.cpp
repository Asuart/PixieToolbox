#define SDL_MAIN_HANDLED

// #include <PixieApplication/Config.h>
// #include <PixieToolboxCore/Scene/Scene.h>

// #include "PixieToolbox.h"

// #include "PixieToolboxCore/Scene/Components/SphereComponent.h"

// using namespace PixieToolbox;

int main(int argc, char** argv) {
    // Config::SetApplicationPath(argv[0]);

    // PixieToolboxApplication app("Pixie Toolbox", { 1280, 720 }, RenderAPI::OpenGL, true);
    //app.Start();

    // Scene* scene = new Scene("Test");

    // ResourceManager* manager = ResourceManager::getInstance();
    // manager->addResource(scene);
    // std::shared_ptr<SceneObject> obj1 = std::make_shared<SceneObject>("obj1");
    // std::shared_ptr<SceneObject> obj2 = std::make_shared<SceneObject>("obj2");
    // std::shared_ptr<SceneObject> obj3 = std::make_shared<SceneObject>("obj3");
    // scene->AddObject(obj1);
    // // scene->AddObject(obj2);
    // // scene->AddObject(obj3);
    // // scene->CreateComponent<SphereComponent>(obj2, 0.0f);

    // Scene* other = manager->getResource<Scene>(scene->getNameHash());


    // std::cout << other->serialize().data();

    return 0;
}
